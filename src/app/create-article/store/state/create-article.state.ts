import {CreateArticleStateInterface} from '../../types/createArticleState.interface';

export const initialCreateArticleState: CreateArticleStateInterface = {
  validationErrors: null,
  isSubmitting: false,
};
